CSV files ──► scripts ──► transactions (Cloud SQL) ──► staging model ──► mart model

CSV files ──► scripts ──► stg_transaction (Cloud SQL) ──► staging model ──► mart model
│
transactions (Cloud SQL) ◄──────────┘ (future: promote clean data)
